---
import BaseLayout from '../layouts/BaseLayout.astro';
import Nav from '../components/Nav.astro';
import Hero from '../components/Hero.astro';
import Footer from '../components/Footer.astro';
---

<BaseLayout
  title="ragweld - Tribrid RAG: Vector + Sparse + Graph"
  description="ragweld combines vector search, sparse retrieval (BM25), and GraphRAG into one unified retrieval system. Experience the power of tribrid RAG."
  canonical="https://ragweld.com/"
  ogImage="/og-image.png"
>
  <Nav />
  <main>
    <Hero />
    <section id="what" class="bg-[#0a0a0a] pb-16">
      <div class="mx-auto max-w-7xl px-4">
        <div class="grid gap-10 md:grid-cols-3">
          <div class="rounded-xl border border-[#222] bg-[#0f0f0f] p-6">
            <div class="text-sm font-semibold text-[#00ff88]">Vector</div>
            <h2 class="mt-2 text-lg font-bold text-white">Semantic retrieval</h2>
            <p class="mt-2 text-sm text-gray-400">
              Finds relevant passages even when the query doesn’t share exact keywords.
            </p>
          </div>
          <div class="rounded-xl border border-[#222] bg-[#0f0f0f] p-6">
            <div class="text-sm font-semibold text-[#5b9dff]">Sparse</div>
            <h2 class="mt-2 text-lg font-bold text-white">BM25 / FTS</h2>
            <p class="mt-2 text-sm text-gray-400">
              High-precision lexical matches for identifiers, error strings, and config keys.
            </p>
          </div>
          <div class="rounded-xl border border-[#222] bg-[#0f0f0f] p-6">
            <div class="text-sm font-semibold text-gray-300">Graph</div>
            <h2 class="mt-2 text-lg font-bold text-white">Relationships</h2>
            <p class="mt-2 text-sm text-gray-400">
              Visualize how modules and symbols connect so you can navigate the corpus like a map.
            </p>
          </div>
        </div>
      </div>
    </section>

    <section id="how" class="bg-[#0a0a0a] pb-20">
      <div class="mx-auto max-w-7xl px-4">
        <div class="mx-auto max-w-3xl text-center">
          <h2 class="text-3xl font-bold text-white">How it works</h2>
          <p class="mt-3 text-gray-400">
            ragweld combines multiple retrieval “lenses” and returns answers with citations.
          </p>
        </div>
        <div class="mt-10 grid gap-6 md:grid-cols-2 lg:grid-cols-3">
          <div class="rounded-xl border border-[#222] bg-[#0f0f0f] p-6">
            <div class="text-xs font-semibold text-gray-400">1</div>
            <div class="mt-2 text-base font-semibold text-white">Index the corpus</div>
            <div class="mt-2 text-sm text-gray-400">
              Chunk files and docs into citeable spans with file paths + line ranges.
            </div>
          </div>
          <div class="rounded-xl border border-[#222] bg-[#0f0f0f] p-6">
            <div class="text-xs font-semibold text-gray-400">2</div>
            <div class="mt-2 text-base font-semibold text-white">Retrieve (multi-leg)</div>
            <div class="mt-2 text-sm text-gray-400">
              Run sparse + (optionally) vector retrieval, plus graph-aware navigation signals.
            </div>
          </div>
          <div class="rounded-xl border border-[#222] bg-[#0f0f0f] p-6">
            <div class="text-xs font-semibold text-gray-400">3</div>
            <div class="mt-2 text-base font-semibold text-white">Answer + cite</div>
            <div class="mt-2 text-sm text-gray-400">
              Generate an answer grounded in the retrieved spans, with citations you can click.
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>
  <Footer />
</BaseLayout>
