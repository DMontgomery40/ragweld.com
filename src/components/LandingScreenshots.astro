---
const screenshots = [
  {
    title: 'Retrieval config (hybrid + dense)',
    description:
      'Tune the core retrieval knobs: BM25 + dense, candidate counts, thresholds, reranking, and advanced fusion behavior.',
    src: '/screenshots/config-retrieval.png',
    alt: 'Configuration screen showing retrieval parameters for hybrid (BM25) and dense (vector) retrieval, plus reranking and advanced tuning.',
  },
  {
    title: 'Chat with knobs + traces',
    description:
      'A chat surface designed for iteration: prompt + response, plus the settings and routing trace you need to explain why an answer happened.',
    src: '/screenshots/chat-tab.png',
    alt: 'Chat tab with a conversation area and a settings panel for chat and retrieval, including routing trace controls.',
  },
  {
    title: 'Embedded Grafana metrics',
    description:
      'Keep observability in the same workspace: configure a Grafana dashboard and monitor throughput, errors, and costs while you run.',
    src: '/screenshots/metrics-embedded-grafana.png',
    alt: 'Metrics screen showing an embedded Grafana dashboard with throughput, error rate, and cost panels.',
  },
];
---

<section id="screenshots" class="bg-[#0a0a0a] pb-20">
  <div class="mx-auto max-w-7xl px-4">
    <div class="mx-auto max-w-3xl text-center">
      <h2 class="text-3xl font-bold text-white">Workbench snapshots</h2>
      <p class="mt-3 text-gray-400">
        A few views you’ll spend time in: keeping the stack healthy, tuning tribrid retrieval, and understanding every
        knob.
      </p>
    </div>

    <div class="mt-10 grid gap-6 lg:grid-cols-3">
      {
        screenshots.map(({ title, description, src, alt }) => (
          <figure class="rounded-xl border border-[#222] bg-[#0f0f0f] overflow-hidden">
            <a href={src} target="_blank" rel="noopener" class="block">
              <img
                src={src}
                alt={alt}
                loading="lazy"
                decoding="async"
                class="h-56 w-full object-cover object-left-top border-b border-[#222]"
              />
            </a>
            <figcaption class="p-6">
              <div class="text-sm font-semibold text-[#00ff88]">{title}</div>
              <p class="mt-2 text-sm text-gray-400">{description}</p>
              <p class="mt-3 text-xs text-gray-500">Click to enlarge.</p>
            </figcaption>
          </figure>
        ))
      }
    </div>
  </div>
</section>
