---
const screenshots = [
  {
    title: 'Chat + dataset context',
    description:
      'Chat with your corpora, inspect sources, and keep retrieval knobs close while you iterate.',
    src: '/screenshots/chat-and-dataset.png',
    alt: 'Chat view alongside a dataset panel, showing conversational results with corpus context and nearby controls.',
  },
  {
    title: 'Indexing settings + Grafana sidecar',
    description:
      'Configure indexing and chunking while watching live metrics in a split-view embedded Grafana dashboard.',
    src: '/screenshots/indexing-settings-with-grafana-on-side.png',
    alt: 'Indexing settings screen shown side-by-side with an embedded Grafana metrics dashboard.',
  },
];
---

<section id="screenshots" class="bg-[#09090b] pb-20">
  <div class="mx-auto max-w-7xl px-4">
    <div class="mx-auto max-w-3xl text-center">
      <h2 class="text-3xl font-bold text-white">Workbench snapshots</h2>
      <p class="mt-3 text-gray-400">
        A few views you’ll spend time in: keeping the stack healthy, tuning tribrid retrieval, and understanding every
        knob.
      </p>
    </div>

    <div class="mt-10 grid gap-6 lg:grid-cols-2">
      {
        screenshots.map(({ title, description, src, alt }) => (
          <figure class="rounded-xl border border-[#27272a] bg-[#0f0f12] overflow-hidden">
            <a href={src} target="_blank" rel="noopener" class="block">
              <img
                src={src}
                alt={alt}
                loading="lazy"
                decoding="async"
                class="h-56 w-full object-cover object-left-top border-b border-[#27272a]"
              />
            </a>
            <figcaption class="p-6">
              <div class="text-sm font-semibold text-[#64748b]">{title}</div>
              <p class="mt-2 text-sm text-gray-400">{description}</p>
              <p class="mt-3 text-xs text-gray-500">Click to enlarge.</p>
            </figcaption>
          </figure>
        ))
      }
    </div>
  </div>
</section>
