---
---
<section id="demo" class="min-h-screen bg-[#09090b] pt-24 pb-12">
  <div class="mx-auto max-w-7xl px-4">
    <div class="text-center mb-10">
      <h1 class="text-5xl md:text-6xl font-bold text-white tracking-tight">
        rag<span class="text-[#64748b]">weld</span>
      </h1>
      <p class="mt-4 text-xl text-gray-400 max-w-2xl mx-auto">
        An open-source RAG workbench: benchmark, debug, and ship.
      </p>
      <p class="mt-2 text-gray-500">
        Hybrid retrieval (vector + sparse + graph) with the engineering surface area you actually need in production.
      </p>
      <p class="mt-2 text-gray-500">
        Now with a trainable SOTA reranker: Qwen3-style <span class="text-gray-400">yes/no logits</span> scoring + LoRA fine-tuning (MLX on Apple Silicon).
      </p>
      <div class="mt-8 flex justify-center gap-4 flex-wrap">
        <a
          href="https://github.com/DMontgomery40/ragweld"
          target="_blank"
          rel="noopener noreferrer"
          class="px-6 py-3 bg-[#64748b] text-white font-semibold rounded-lg hover:bg-[#475569] transition-colors"
        >
          View on GitHub
        </a>
        <a
          href="https://dmontgomery40.github.io/ragweld/latest/configuration/"
          target="_blank"
          rel="noopener noreferrer"
          class="px-6 py-3 border border-[#27272a] text-white font-semibold rounded-lg hover:bg-[#18181b] transition-colors"
        >
          Docs
        </a>
        <a
          href="https://github.com/DMontgomery40/ragweld#quick-start"
          target="_blank"
          rel="noopener noreferrer"
          class="px-6 py-3 border border-[#27272a] text-white font-semibold rounded-lg hover:bg-[#18181b] transition-colors"
        >
          Get Started
        </a>
      </div>
    </div>

    <!-- Feature pills -->
    <div class="flex justify-center gap-3 mb-8 flex-wrap">
      <span class="px-3 py-1 rounded-full bg-[#18181b] text-[#94a3b8] text-sm border border-[#27272a]">
        Vector + Sparse + Graph
      </span>
      <span class="px-3 py-1 rounded-full bg-[#18181b] text-[#94a3b8] text-sm border border-[#27272a]">
        Benchmarks + Evals
      </span>
      <span class="px-3 py-1 rounded-full bg-[#18181b] text-[#94a3b8] text-sm border border-[#27272a]">
        Eval Drilldowns
      </span>
      <span class="px-3 py-1 rounded-full bg-[#18181b] text-[#94a3b8] text-sm border border-[#27272a]">
        Run Diffs + Analysis
      </span>
      <span class="px-3 py-1 rounded-full bg-[#18181b] text-[#94a3b8] text-sm border border-[#27272a]">
        Trainable Reranker (LoRA)
      </span>
      <span class="px-3 py-1 rounded-full bg-[#18181b] text-[#94a3b8] text-sm border border-[#27272a]">
        Grafana Split-view
      </span>
      <span class="px-3 py-1 rounded-full bg-[#18181b] text-[#94a3b8] text-sm border border-[#27272a]">
        Webhooks + Alerts
      </span>
      <span class="px-3 py-1 rounded-full bg-[#18181b] text-[#94a3b8] text-sm border border-[#27272a]">
        Memory + Multi-corpus
      </span>
      <span class="px-3 py-1 rounded-full bg-[#18181b] text-[#94a3b8] text-sm border border-[#27272a]">
        Multi-model + Vision
      </span>
      <span class="px-3 py-1 rounded-full bg-[#18181b] text-[#94a3b8] text-sm border border-[#27272a]">
        MCP Server
      </span>
    </div>

    <!-- Demo iframe container -->
    <div class="rounded-xl border border-[#27272a] overflow-hidden shadow-2xl shadow-black/50 bg-[#0f0f12]">
      <div class="flex items-center justify-between gap-2 px-4 py-3 bg-[#0f0f12] border-b border-[#27272a]">
        <div class="flex items-center gap-2">
          <div class="flex gap-2">
            <div class="w-3 h-3 rounded-full bg-[#ff5f56]"></div>
            <div class="w-3 h-3 rounded-full bg-[#ffbd2e]"></div>
            <div class="w-3 h-3 rounded-full bg-[#27c93f]"></div>
          </div>
          <span class="ml-4 text-gray-500 text-sm">Live Demo (Epstein Files 1 corpus)</span>
        </div>
        <div class="flex items-center gap-3">
          <div class="flex items-center gap-2">
            <a
              href="/demo/?corpus=epstein-files-1"
              class="rounded-md border border-[#27272a] px-2.5 py-1 text-xs text-white hover:bg-[#18181b] transition-colors"
            >
              Epstein Files 1
            </a>
          </div>
          <a
            href="/demo/?corpus=epstein-files-1"
            target="_blank"
            rel="noopener"
            class="rounded-md border border-[#27272a] px-3 py-1.5 text-xs text-white hover:bg-[#18181b] transition-colors"
          >
            Try fullscreen
          </a>
        </div>
      </div>
      <div class="demo-scale-wrapper">
        <iframe
          title="ragweld Demo"
          src="/demo/?corpus=epstein-files-1"
          loading="lazy"
          class="demo-iframe border-0"
          sandbox="allow-scripts allow-same-origin allow-forms"
        ></iframe>
      </div>
    </div>

    <style>
      /* Render the iframe at a larger internal viewport, then scale down
         so the demo UI looks "zoomed out" and the dock isn't cramped. */
      .demo-scale-wrapper {
        position: relative;
        width: 100%;
        overflow: hidden;
        /* Ultrawide ratio — matches the demo's actual content height after scaling */
        aspect-ratio: 21 / 9;
      }

      .demo-iframe {
        position: absolute;
        top: 0;
        left: 0;
        /* Render at ~143% of container → effective ~1830px at max-w-7xl.
           Then scale(0.7) shrinks it back to fit, giving a "zoomed out" look. */
        width: 142.857%;
        height: 142.857%;
        transform: scale(0.7);
        transform-origin: top left;
      }

      /* Slightly less aggressive zoom on smaller screens */
      @media (max-width: 768px) {
        .demo-iframe {
          width: 125%;
          height: 125%;
          transform: scale(0.8);
        }
      }
    </style>

    <p class="mt-4 text-center text-gray-500 text-sm">
      Live search + chat are backed by the Epstein Files 1 corpus. Add <span class="text-gray-400">?mock=1</span> for offline demo mode.
    </p>
  </div>
</section>
